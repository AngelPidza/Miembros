import 'package:flutter/material.dart';

class AppColors {
  // Colores Primarios
  static const Color primaryColor = Color(0xFF176087);
  static const Color secondaryColor = Color.fromARGB(255, 197, 224, 234);

  // Colores de Fondo
  static const Color backgroundColor = Color(0xFF0A2239);
  static const Color cardColor = Color(0xFF132E32);
  static const Color onlyColor = Color.fromARGB(255, 180, 196, 204);

  // Colores de Texto
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color.fromARGB(255, 94, 91, 91);

  // Colores de Errores
  static const Color errorColor = Color(0xFFB00020);
}
